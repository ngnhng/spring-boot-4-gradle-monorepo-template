package com.onboard.registration.domain.service;

import com.onboard.registration.domain.vo.FormData;

/** Domain service for merging registration form data. */
public final class FormMergeService {
  /** Merges form data according to domain merge rules. */
  public static FormData merge(FormData data) {
    return null;
  }
}
